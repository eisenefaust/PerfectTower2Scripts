:import IronLagSaver_lib
:name {package(Toggle (K))}

key.k()

gotoif(4, LagSaver == "")
gotoif(7, LagSaver == "Press k to stop")
goto(7)
LagSaver = "Press k to stop"
execute("{package(main)}")
goto(99)
gu("LagSaver")
stop("{package(main)}")
